@model NGL.Web.Models.Section.CreateModel

           
@{
    ViewBag.Title = "Create a Section";
}

<h2>Section</h2>

@using (var f = Html.BeginNglForm())
{
    using (var s = f.BeginSection("Info"))
    {
        <div class="hidden">
        @s.FieldFor(m => m.SchoolYear)
        @s.FieldFor(m => m.Term)
        </div>
        @s.FieldFor(m => m.Session)        
        @s.FieldFor(m => m.Period)
        @s.FieldFor(m => m.Classroom)
        
        @Html.Label("Course")
        @Html.TextBox("Course") 
    
        @s.FieldFor(m => m.UniqueSectionCode)
        @s.FieldFor(m => m.SequenceOfCourse)
    
    }
    
    using (var n = f.BeginNavigation())
    {
        @n.Submit("Create")
    }
}

@section Scripts {
    Ngl.section.getSession.init();
    Ngl.section.getCourse.init();
}
